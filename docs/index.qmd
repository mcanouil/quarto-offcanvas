---
title: "Offcanvas"
subtitle: "Sliding panels from a fenced div."
description: "What the offcanvas extension is for, how to enable it, and the smallest div that produces a panel and its trigger."
authors:
  - name: "Mickaël CANOUIL, _Ph.D._"
    orcid: "0000-0002-3396-4549"
    url: "https://mickael.canouil.fr"
---

A Bootstrap offcanvas is a panel that slides in from an edge of the window, and writing one by hand means several nested divs, a set of `data-bs-` attributes, a separate trigger element, and the ARIA wiring between them.

`offcanvas` builds all of that from a fenced div.
The div becomes the panel, its first heading becomes the title, and the trigger is written for you.
The content stays markdown.

## Installation

```bash
quarto add {{< var extension.reference >}}
```

This will install the extension under the `_extensions` subdirectory.
If you are using version control, you will want to check in this directory.

Or install it from your editor with [Quarto Wizard](https://m.canouil.dev/quarto-wizard):

::: {.btn-group role="group" aria-label="Quarto Wizard installer options"}

[Visual Studio Code](vscode://mcanouil.quarto-wizard/install?repo={{< var extension.wizard-reference >}}){.btn .btn-outline-primary title="Install {{< var extension.reference >}} in Visual Studio Code using the Quarto Wizard extension."}
[Positron](positron://mcanouil.quarto-wizard/install?repo={{< var extension.wizard-reference >}}){.btn .btn-outline-primary title="Install {{< var extension.reference >}} in Positron using the Quarto Wizard extension."}
[VSCodium](vscodium://mcanouil.quarto-wizard/install?repo={{< var extension.wizard-reference >}}){.btn .btn-outline-primary title="Install {{< var extension.reference >}} in VSCodium using the Quarto Wizard extension."}

:::

## Quick start

Enable the filter:

```yaml
filters:
  - offcanvas
```

Then write the panel:

```markdown
:::: {.offcanvas trigger-text="Open the panel"}

# A panel

Written as ordinary markdown.

::::
```

Which gives you this:

:::: {.offcanvas trigger-text="Open the panel"}

# A panel

Written as ordinary markdown, so a [link](https://quarto.org) or a list behaves as it would anywhere else.

::::

See the [Reference](reference.qmd) for every attribute, and the [Examples](examples.qmd) for panels from each edge.

## Where it works

HTML formats carrying Bootstrap 5, which is Quarto's default `html` and its website and book projects.

::: {.callout-note}
Bootstrap 4 and earlier have no offcanvas component, and the extension produces nothing there.
The responsive breakpoints need Bootstrap 5.2; Quarto ships 5.3.
:::

## Links

- [Reference](reference.qmd) for every attribute.
- [Examples](examples.qmd) for working panels.
- [Changelog](changelog.qmd) for what each release changed.
- [Source and issues](https://github.com/mcanouil/quarto-offcanvas) on GitHub.
